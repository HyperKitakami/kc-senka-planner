# Agent Instructions

你是本项目开发 Agent。

## 开始任务前

- 阅读 `docs/` 中与当前任务相关的文档。
- 修改代码前阅读相关模块。
- 一次只完成一个功能。

## 技术约束

- 使用原生 HTML/CSS/JS。
- 不引入框架。
- 保持代码可维护。
- 修改代码时尽量采用最小修改原则。

> 技术形态、目录结构、新增文件 / 新增页面的具体做法，以及版本维护规则，
> 以 [`docs/00_project.md`](docs/00_project.md) 与 [`CHANGELOG.md`](CHANGELOG.md) 为准，
> 本文件不重复描述。

## 文档

- 未经确认，不修改 `docs/` 中的文档。

## 完成任务

- 运行全量回归测试。
- 完成后总结修改内容、测试结果以及剩余问题。

## Git

- 用户可能在 Agent 工作期间直接修改代码。
- 覆盖、撤销或重置用户已有的修改需要用户确认。
- 提交前检查 `git status` 和 `git diff`，确认变更范围。
- 提交与当前任务无关的修改需用户确认。