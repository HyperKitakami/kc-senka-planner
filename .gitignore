# ---------------------------------------------------------------------------
# AI 协作工具的状态目录
# 内含 memory/（设计决策与项目约定）、analysis/、review/、tests/（本地回归测试）
# 以及 tmp-*.html 预览稿。均属「工具状态」而非项目源码，故整体不入库。
# ⚠️ 测试套件（tests/）与 memory/ 一样只存在于本机，
#    换机器或清掉该目录后无法复现回归测试，需要留存请另行备份。
# ---------------------------------------------------------------------------
.workbuddy-ai/

# ---------------------------------------------------------------------------
# 依赖
# 本项目的依赖装在隔离工作区而非项目内，这两个规则是防御性的：
# 避免将来在项目根目录跑 npm install 时误提交。
# ---------------------------------------------------------------------------
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# ---------------------------------------------------------------------------
# 编辑器 / IDE
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# ---------------------------------------------------------------------------
# 操作系统
# ---------------------------------------------------------------------------
Thumbs.db
Desktop.ini
.DS_Store
$RECYCLE.BIN/

# ---------------------------------------------------------------------------
# 日志与临时文件
# ---------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tmp

# ---------------------------------------------------------------------------
# 测试与构建产物
# ---------------------------------------------------------------------------
coverage/
dist/
build/
