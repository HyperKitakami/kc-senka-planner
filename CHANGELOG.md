# 更新日志

Kancolle Senka Planner 的版本与开发历史。

> 本文件只记录**版本号与每个版本做了什么**。
> 技术形态、目录结构、开发约定见 [`docs/00_project.md`](docs/00_project.md)；
> Agent 行为约束见 [`AGENTS.md`](AGENTS.md)。

---

## 版本维护规则

程序版本号有 **三个落点，发布时必须成对更新**：

1. [`js/core/version.js`](js/core/version.js) 的 `VERSION`（唯一代码载体，设置页「关于」读它）
2. 本文件的「当前版本」
3. git tag

### 程序版本 vs 数据结构版本

两者含义不同，**不要混用**：

| 名称 | 载体 | 含义 |
| --- | --- | --- |
| 程序版本 | `js/core/version.js` 的 `VERSION` | 给人看的语义化版本 |
| 数据结构版本 | `KC.schema.SCHEMA_VERSION` | 给迁移链看的版本，导出文件里存的是它 |

程序版本变了而数据结构没变时，**不要**动 `SCHEMA_VERSION`，否则会平白触发一次数据迁移。

---

## 当前版本

**`v0.3.1`**

---

## 开发历史

| 版本 | 内容 |
| --- | --- |
| `v0.1.0`（`bc0e93b`） | 8 个导航页全部实现 |
| — | 陆续新增：战果记录补录、首页卡片布局、周期查询页、归档分页与筛选、月度比较可选数据项、归档覆盖保护、定期导出提醒、任务进度节点、战果记录页双录入模式（列表 / 日历） |
| `v0.2.0`（`c517a4f`） | 接入 poi 战果插件数据：每日建议值与一键填充、战果线对比图、EO 完成状态批量同步、快照管理 |
| `v0.3.0` | 新增程序版本号载体 `js/core/version.js`，并在「设置 → 关于」中展示程序版本与数据结构版本；清理源码中指向已移除文档的注释 |
| `v0.3.1` | 新增内置「预设 EX 任务」（季常 7 项 / 年常 2 项），在任务页 EO 卡片下方单独成卡片；接入 poi 季常完成状态批量同步（仅季常，年常手动勾选）；任务名称列改为显示游戏内完整任务名（简称保留为悬停提示）；内置任务支持升级时补齐新增的展示字段 |
